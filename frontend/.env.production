# Продакшен-сборка (`npm run build` / `npm run preview`).
# Пока бэкенд и AI-сервис поднимаются локально в Docker/uvicorn —
# те же адреса, что и в .env.development.
VITE_API_BASE_URL=http://localhost:8080
VITE_AI_API_BASE_URL=http://localhost:8000
