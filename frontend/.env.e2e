# Режим сборки для e2e: бэкенд подменяется браузерным MSW,
# поэтому Go-сервер и база для сквозных сценариев не нужны.
# Используется командой `npm run build:e2e` (vite build --mode e2e).
VITE_ENABLE_MSW=true
VITE_API_BASE_URL=http://api.test
VITE_AI_API_BASE_URL=http://ai.test
